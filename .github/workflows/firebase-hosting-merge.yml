# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: 'Select project to deploy'
        required: true
        default: 'TorresSanSebastian'
        type: choice
        options:
          - TorresSanSebastian
          - Alameda181

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      env_name: ${{ steps.set_project.outputs.env_name }}
    steps:
      - id: set_project
        run: |
          ENV_NAME="${{ github.event.inputs.project }}"
          echo "env_name=$ENV_NAME" >> $GITHUB_OUTPUT

  build:
    needs: setup
    uses: ./.github/workflows/reusable-build.yml
    with:
      environment: ${{ github.event.inputs.environment }}
    secrets:
      PROPERTIES_REPO_TOKEN: ${{ secrets.PROPERTIES_REPO_TOKEN }}

  deploy:
    needs: [setup, build]
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment: ${{ github.event.inputs.environment }}
    secrets:
      FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}

  tag:
    needs: [build, deploy]
    permissions:
      contents: write
    uses: ./.github/workflows/reusable-tag.yml
    with:
      version: ${{ needs.build.outputs.version }}
